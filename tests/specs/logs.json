[
  {
    "name": "Activity log gives a useful empty state",
    "steps": [
      { "send": { "callback": "logs:show" }, "expect": [
        { "method": "editMessageText", "payload": { "text": "No activity yet — add a sub-bot or register a channel to get started." } }
      ] }
    ]
  }
]
