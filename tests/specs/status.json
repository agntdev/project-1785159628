[
  {
    "name": "Status gives a useful empty state",
    "steps": [
      { "send": { "callback": "status:show" }, "expect": [
        { "method": "editMessageText", "payload": { "text": "Nothing is connected yet — add a sub-bot, then register a channel." } }
      ] }
    ]
  }
]
