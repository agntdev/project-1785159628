[
  {
    "name": "Remove sub-bot shows an empty state",
    "steps": [
      { "send": { "callback": "bots:remove" }, "expect": [
        { "method": "editMessageText", "payload": { "text": "No sub-bots yet — tap Add sub-bot to create one." } }
      ] }
    ]
  }
]
