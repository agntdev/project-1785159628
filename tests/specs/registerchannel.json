[
  {
    "name": "Register channel requests its address",
    "steps": [
      { "send": { "callback": "channels:add" }, "expect": [
        { "method": "editMessageText", "payload": { "text": "Send the channel or group @username, or its numeric chat ID." } }
      ] }
    ]
  }
]
